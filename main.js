
//Making the Game
var prompt = require('prompt');

prompt.start();

var Word = require('./word.js');

var game = {
	wordBank : 
	guessesRemaining : "10";
	currentWrd :"null";

	function startGame(wrd) {

		var new Word = (wordBank)
		new Word = currentWrd
	}
}
